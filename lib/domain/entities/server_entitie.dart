import 'package:conquer/domain/infrastructure/conquer_products.dart';

class Server {
    final int id;
    final String label;
    final ServerName name;

    Server({
        required this.id,
        required this.label,
        required this.name,
    });
}